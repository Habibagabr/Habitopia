<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:ignore="MissingDefaultResource"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#F2EADF">
    <!-- Intro Text (Outside the Green Rectangle, Top-Left) -->
    <TextView
        android:id="@+id/introText"
        android:layout_width="match_parent"
        android:layout_height="128dp"
        android:textAlignment="center"
        android:padding="5dp"
        android:layout_alignParentStart="true"
        android:layout_alignParentTop="true"
        android:layout_marginTop="26dp"
        android:text="@string/intro_text"
        android:textColor="#27767B"
        android:textSize="25sp" />

    <!-- Progress Bar (Two Rectangles Below Intro Text) -->
    <View
        android:id="@+id/progressBackground"
        android:layout_width="match_parent"
        android:layout_height="20dp"
        android:layout_below="@id/introText"
        android:layout_marginStart="20dp"
        android:layout_marginEnd="20dp"
        android:layout_marginTop="-30dp"
        android:background="@drawable/progress_background"
        android:contentDescription="@string/progress_background_description"/>

    <!-- Inner RelativeLayout for progressForeground and the number "4" -->
    <RelativeLayout
        android:id="@+id/progressContainer"
        android:layout_width="wrap_content"
        android:layout_height="20dp"
        android:layout_alignStart="@id/progressBackground"
        android:layout_alignTop="@id/progressBackground"
        android:layout_alignBottom="@id/progressBackground"
        android:contentDescription="@string/progress_container_description">

        <View
            android:id="@+id/progressForeground"
            android:layout_width="270dp"
            android:layout_height="20dp"
            android:background="@drawable/progress_foreground"
            android:contentDescription="@string/progress_foreground_description"
            tools:ignore="UnusedAttribute"/>

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignEnd="@id/progressForeground"
            android:layout_alignTop="@id/progressForeground"
            android:layout_marginEnd="4dp"
            android:layout_marginTop="-4dp"
            android:gravity="center"
            android:text="@string/progress_number_4"
            android:textColor="#27767B"
            android:textSize="20sp"
            android:contentDescription="@string/progress_number_4_description"/>
    </RelativeLayout>

    <!-- Main Content (Question and Answers in a Green Rectangle) -->
    <LinearLayout
        android:id="@+id/greenRectangle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_centerVertical="true"
        android:layout_marginStart="20dp"
        android:layout_marginEnd="20dp"
        android:background="@android:color/transparent"
        android:orientation="vertical"
        android:padding="20dp"
        android:contentDescription="@string/green_rectangle_description">

        <!-- Green Rectangle Background for Question and Answers -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="400dp"
            android:background="@drawable/rounded_button"
            android:backgroundTint="@color/green"
            android:orientation="vertical"
            android:padding="20dp">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="start"
                android:layout_marginTop="2dp"
                android:text="@string/q4_label"
                android:textColor="#F2EADF"
                android:textSize="24sp"
                android:contentDescription="@string/q4_label_description" />

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center"
                android:layout_marginTop="8dp"
                android:text="@string/q4_text"
                android:textColor="#F2EADF"
                android:textSize="25sp"
                android:contentDescription="@string/q4_text_description"/>

            <!-- Number Input Field for Q4 -->
            <EditText
                android:id="@+id/waterCupsInput"
                android:layout_width="120dp"
                android:layout_height="60dp"
                android:layout_gravity="center"
                android:layout_marginTop="16dp"
                android:background="@drawable/rounded_rectangle_background"
                android:gravity="center"
                android:inputType="number"
                android:textColor="@android:color/black"
                android:textSize="20sp"
                android:hint="@string/water_cups_input_hint"
                android:autofillHints="number"/>
        </LinearLayout>
    </LinearLayout>

    <!-- Continue Button (Outside the Green Rectangle) -->
    <Button
        android:id="@+id/continue_button"
        android:layout_width="200dp"
        android:layout_height="60dp"
        android:layout_below="@id/greenRectangle"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="20dp"
        android:background="@drawable/rectangle_button_background"
        app:backgroundTint="@null"
        style="@style/Widget.AppCompat.Button"
        android:text="@string/continue_text"
        android:textSize="20sp"
        android:textColor="#F2EADF"
        android:contentDescription="@string/continue_button_description" />

    <!-- Skip Text at the Bottom Right -->
    <TextView
        android:id="@+id/skip_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_alignParentEnd="true"
        android:layout_marginEnd="20dp"
        android:layout_marginBottom="20dp"
        android:text="@string/skip"
        android:padding="16dp"
        android:textColor="#27767B"
        android:textSize="30sp"
        android:clickable="true"
        android:focusable="true"
        android:contentDescription="@string/skip_description" />
</RelativeLayout>